name: Job Scraper (ATS feeds)

on:
  schedule:
    # every hour
    - cron: "17 * * * *"
  workflow_dispatch: {}

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        env:
          # Optional: for Slack/email hooks you add later
          # SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TZ: America/Toronto
        run: |
          python scrape_jobs.py --config config.yaml --db jobs.db

      - name: Upload DB artifact
        uses: actions/upload-artifact@v4
        with:
          name: jobs-db
          path: jobs.db
          retention-days: 14

      # OPTIONAL: commit updated DB to repo (works, but DB diffs can be noisy)
      # - name: Commit DB
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"
      #     git add jobs.db
      #     git commit -m "Update jobs.db" || exit 0
      #     git push
